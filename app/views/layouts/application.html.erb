<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#F4F5F9]">
    <% if flash.any? %>
      <div>
        <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block"><%= notice %></p>
        <p class="py-2 px-3 bg-green-50 mb-5 text-red-500 font-medium rounded-lg inline-block"><%= alert %></p>
      </div>
    <% end %>
    <nav>
      <% if user_signed_in? %>
        <%= render "layouts/navbar" %>
      <% end %>
    </nav>
    <%= yield %>
  </body>
</html>
